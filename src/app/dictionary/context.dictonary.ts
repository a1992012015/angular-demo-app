/**
 * local storage version
 */
export const LOCAL_STORAGE_VERSION = '0.0.6';

/**
 * max integer
 */
export const MAX_INTEGER = Math.pow(2, 31);

/**
 * unit system identifiers
 */
export enum UnitSystem {
  IMPERIAL,
  METRIC
}
