import { AttachHostDirective } from './attach-host.directive';

describe('AttachHostDirective', () => {
  it('should create an instance', () => {
    const directive = new AttachHostDirective();
    expect(directive).toBeTruthy();
  });
});
